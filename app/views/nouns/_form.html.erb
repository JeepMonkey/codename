<%= simple_form_for @noun, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :noun %>
  <%= error_span(@noun[:noun]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                nouns_path, :class => 'btn btn-default' %>
<% end %>
